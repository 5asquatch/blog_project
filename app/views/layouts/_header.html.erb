  <nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid"><article>
  <header>
    <h1>Instagram </h1>
    <% if current_user.avatar.present? %>
        <%= image_tag(current_user.avatar, height: 60, width: 60, :class=> "img-circular") %>
        <% else %>
        <%= image_tag "default-avatar.png", height: 60, width: 60, :class=> "img-circular"%> 
    <% end %> 
    <b> <%= current_user.name%> </b> <%= link_to "Followers: #{current_user.followers.count} ", "/followers/#{current_user.id}"%>  <%= link_to "Subscribes: #{current_user.followees.count} ", "/subscribes/#{current_user.id}"%>
    <p><%= link_to "News  ", root_path%>
    <%= link_to "My page", user_path(current_user.id)%>
    <%= link_to "Settings  ", edit_user_registration_path%>
    <%= link_to "Create Post", new_post_path%>
    <%= button_to "Log out", destroy_user_session_path, :method => :delete %>
    <div class = "search">
      <h5>Search user</h5>
      <%= form_tag(search_path, method: :get) do %>
        <%= text_field_tag(:search, params[:name]) %>
        <%= submit_tag ("Search")%>
      <%end%>
    </div>
     </div> 
  </header>
</article> 
