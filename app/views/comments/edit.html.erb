<h1>Edit comment</h1>
<%= form_for [@post, @comment] do |f| %>
    <div>
    <%= f.label :body %>
    <%= f.text_area :body %>
    </div>
    <%= f.submit %>
<% end %>
<%= link_to "Back", post_path(@post) %>